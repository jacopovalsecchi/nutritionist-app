generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Step 1: Prisma is wired to Postgres. Domain models start in later steps.
